stages:
  - build
build:
  script:
    - echo build
  stage: build
include:
  - local: .gitlab/base.yml
  - template: Jobs/Build.gitlab-ci.yml
  - file: .gitlab-ci.yml
    project: group/platform
    ref: main
