on:
  workflow_dispatch:
    inputs:
      target:
        required: true
        type: string
permissions: {}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - id: echo
        run: echo hi
