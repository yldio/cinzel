# generated-by: cinzel
# cinzel-provider: github
name: CI
on:
  push:
permissions: {}
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - id: echo
        run: echo hi
  build:
    runs-on: ubuntu-latest
    steps:
      - id: echo
        run: echo hi
  deploy:
    runs-on: ubuntu-latest
    steps:
      - id: echo
        run: echo hi
