# generated-by: cinzel
# cinzel-provider: github
name: Build
run-name: "Build #1"
on:
  push:
permissions: {}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - id: echo
        run: echo hi
